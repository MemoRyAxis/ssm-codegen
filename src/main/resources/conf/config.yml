db:
  driver:
  url:
  username:
  password:

project:
  home:
  name:
  buildTool:
  mainPackage:
  utilPackage:
  modulePackage:

export:
  models:
    - model: po
#    - model: dao
#    - model: daoXml
    - model: mapper
    - model: mapperXml
    - model: controller
    - model: service

tables:
  - tableName:
    javaType:

